{
  "frontend/src/index.css": "Global styles with dark athletic theme using OKLCH color tokens updated to a green (primary) and yellow (accent) palette, Barlow Condensed/Barlow fonts, glow utilities, and keyframe animations for the Powerpal fitness app.",
  "frontend/tailwind.config.js": "Extended Tailwind config updated with vibrant green (primary) and bright yellow (accent) color tokens, Barlow athletic font families, green/yellow glow box-shadows, and dark fitness brand design tokens.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — useGetCallerUserProfile (with actor-aware loading/fetched states), useSaveCallerUserProfile, useGetProfile, useUpdateProfile (now delegates to saveCallerUserProfile), useGetWorkoutPlan, useGenerateWorkoutPlan, useGetMealPlan, and useGenerateMealPlan — all with proper error handling and cache invalidation.",
  "frontend/src/components/PowerpalMascot.tsx": "Reusable animated Powerpal mascot component with speech bubble, context-aware rotating messages, and a simple letter fallback. Renders the user-provided Hulk image (`/assets/IMG_0972.webp`) with adjusted height proportions to match the portrait aspect ratio, with a drop-shadow glow effect.",
  "frontend/src/components/OnboardingFlow.tsx": "Multi-step onboarding form (personal info → fitness level → goal) that saves profile and generates plans to localStorage using `mutateAsync` for all three operations so errors are properly caught, with PowerpalMascot and step progress indicator.",
  "frontend/src/components/WorkoutPlanView.tsx": "Weekly workout plan display with a green gradient banner overlay for the \"YOUR WORKOUT PLAN\" section, localStorage-based hooks, Build/Regenerate button, navigation to all other views, PowerpalMascot, and the header logo styled with the green CSS filter.",
  "frontend/src/pages/HomePage.tsx": "Landing page with hero section, Hulk mascot, and CTA buttons; the Powerpal logo now has a green CSS filter (hue-rotate + saturate) applied to render it in green, consistent with the app's neon green theme.",
  "frontend/src/App.tsx": "Root app component that checks localStorage for an existing profile via useGetProfile and renders OnboardingFlow for new users or the appropriate view component for returning users, with no authentication logic.",
  "frontend/src/components/MealPlanView.tsx": "7-day meal plan display using localStorage-based hooks, with day filter pills, macro badges, regenerate button, navigation to all other views, and the header logo now styled with the green CSS filter.",
  "frontend/src/components/AdaptiveLevelsView.tsx": "Fitness level selector using localStorage-based hooks that saves updated profile and regenerates the workout plan locally, with level criteria display, success/error feedback, and the header logo now styled with the green CSS filter.",
  "frontend/src/components/WeeklyScheduleView.tsx": "Weekly schedule view using localStorage-based hooks, with expandable day cards, per-day completion toggles, progress bar, navigation to all other views, and the header logo now styled with the green CSS filter.",
  "frontend/src/components/GoalDrivenPlansView.tsx": "Goal selection view using localStorage-based hooks that saves updated profile and regenerates both workout and meal plans locally, with PowerpalMascot and the header logo now styled with the green CSS filter consistent with the app theme.",
  "frontend/src/components/LoginScreen.tsx": "New full-page login screen shown to unauthenticated users, featuring the Powerpal mascot/logo, feature highlights, and an Internet Identity login button with loading state.",
  "frontend/src/components/ProfileSetupModal.tsx": "New full-page profile setup flow (personal info → fitness level → goal) shown to authenticated users who have no saved profile, calling saveCallerUserProfile then generateWorkoutPlan on completion.",
  "frontend/src/utils/localStorage.ts": "Type-safe localStorage utility with custom BigInt-aware JSON serializer/deserializer (using `__bigint__` wrapper objects) so that Profile, WeeklyPlan, and WeeklyMealPlan objects with bigint fields can be safely stored and retrieved without errors.",
  "frontend/src/utils/planGenerator.ts": "Frontend plan generation module that creates WeeklyPlan and WeeklyMealPlan objects from a Profile using rule-based logic matching the backend's generation approach, with bigint values matching backend types.",
  "frontend/src/hooks/useLocalQueries.ts": "React Query hooks (useGetProfile, useSaveProfile, useGetWorkoutPlan, useGenerateWorkoutPlan, useGetMealPlan, useGenerateMealPlan) that read/write from localStorage instead of the backend actor.",
  "frontend/src/main.tsx": "Application entry point that renders the App component without any InternetIdentityProvider wrapper, using only React StrictMode."
}