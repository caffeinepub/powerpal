{
  "frontend/src/index.css": "Global styles with dark athletic theme using OKLCH color tokens updated to a green (primary) and yellow (accent) palette, Barlow Condensed/Barlow fonts, glow utilities, and keyframe animations for the Powerpal fitness app.",
  "frontend/tailwind.config.js": "Extended Tailwind config updated with vibrant green (primary) and bright yellow (accent) color tokens, Barlow athletic font families, green/yellow glow box-shadows, and dark fitness brand design tokens.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — useGetCallerUserProfile (with actor-aware loading/fetched states), useSaveCallerUserProfile, useGetProfile, useUpdateProfile (now delegates to saveCallerUserProfile), useGetWorkoutPlan, useGenerateWorkoutPlan, useGetMealPlan, and useGenerateMealPlan — all with proper error handling and cache invalidation.",
  "frontend/src/components/PowerpalMascot.tsx": "Reusable animated Powerpal mascot component with speech bubble, context-aware rotating messages, and a simple letter fallback. Renders the user-provided Hulk image (`/assets/IMG_0972.webp`) with adjusted height proportions to match the portrait aspect ratio, with a drop-shadow glow effect.",
  "frontend/src/components/OnboardingFlow.tsx": "Single-step onboarding form that collects only name and age, saves a profile with default fitnessLevel (beginner) and goal (generalFitness) to localStorage, then generates workout and meal plans before calling onComplete.",
  "frontend/src/components/WorkoutPlanView.tsx": "Weekly workout plan display using localStorage hooks (useGetProfile, useGetWorkoutPlan, useGenerateWorkoutPlan) instead of backend hooks, with banner, mascot, build/regenerate button, and navigation.",
  "frontend/src/pages/HomePage.tsx": "Landing page with hero section, Hulk mascot, and CTA buttons; the Powerpal logo now has a green CSS filter (hue-rotate + saturate) applied to render it in green, consistent with the app's neon green theme.",
  "frontend/src/App.tsx": "Root app component that checks localStorage for an existing profile (via useGetProfile), shows the simplified OnboardingFlow when no profile exists, and renders the appropriate view component otherwise — no Internet Identity authentication required.",
  "frontend/src/components/MealPlanView.tsx": "7-day meal plan display using localStorage hooks (useGetProfile, useGetMealPlan, useGenerateMealPlan) instead of backend hooks, with day filter pills, macro badges, and regenerate button.",
  "frontend/src/components/AdaptiveLevelsView.tsx": "Fitness level selector using localStorage hooks (useGetProfile, useSaveProfile, useGenerateWorkoutPlan) to update the profile and regenerate the workout plan locally without backend authentication.",
  "frontend/src/components/WeeklyScheduleView.tsx": "Weekly schedule view with expandable day cards, per-day completion toggles, and progress bar — now using localStorage hooks (useGetProfile, useGetWorkoutPlan, useGenerateWorkoutPlan) instead of backend hooks.",
  "frontend/src/components/GoalDrivenPlansView.tsx": "Goal selection view using localStorage hooks (useGetProfile, useSaveProfile, useGenerateWorkoutPlan, useGenerateMealPlan) to update the profile and regenerate plans locally without backend authentication.",
  "frontend/src/components/LoginScreen.tsx": "Full-page login screen for unauthenticated users featuring the Powerpal logo, mascot image, feature highlights list, and an Internet Identity login button with loading state and proper error handling.",
  "frontend/src/components/ProfileSetupModal.tsx": "Full-page 3-step profile setup flow (personal info → fitness level → goal) shown to authenticated users with no saved profile, calling saveCallerUserProfile then generateWorkoutPlan and generateMealPlan on completion using backend hooks.",
  "frontend/src/utils/localStorage.ts": "Type-safe localStorage utility with custom BigInt-aware JSON serializer/deserializer (using `__bigint__` wrapper objects) so that Profile, WeeklyPlan, and WeeklyMealPlan objects with bigint fields can be safely stored and retrieved without errors.",
  "frontend/src/utils/planGenerator.ts": "Frontend plan generation module that creates WeeklyPlan and WeeklyMealPlan objects from a Profile using rule-based logic matching the backend's generation approach, with bigint values matching backend types.",
  "frontend/src/hooks/useLocalQueries.ts": "React Query hooks for localStorage-based profile and plan operations; useGenerateWorkoutPlan and useGenerateMealPlan now accept an optional Profile argument (falling back to reading from localStorage) so they can be called both during onboarding and from view components.",
  "frontend/src/main.tsx": "Application entry point that renders the App component without any InternetIdentityProvider wrapper, using only React StrictMode."
}